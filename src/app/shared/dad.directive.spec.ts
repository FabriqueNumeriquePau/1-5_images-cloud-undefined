import { DadDirective } from './dad.directive';

describe('DadDirective', () => {
  it('should create an instance', () => {
    const directive = new DadDirective();
    expect(directive).toBeTruthy();
  });
});
